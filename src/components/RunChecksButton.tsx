import React from 'react';
import Button from '@mui/material/Button';

const RunChecksButton = (): JSX.Element => (
  <Button id="run-checks-button">Run Npm Command</Button>
);

export default RunChecksButton;
