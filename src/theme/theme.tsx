import { createTheme } from "@mui/material";

const theme = createTheme ({
    palette: {
        primary: {
            main: '#2E2644',
            light: "#D0C9D6",
        },
        secondary: {
            main: "#6CE5E5",
            light: "#FFFFFF",
            dark: "#230A5E"
        }
    }
})

export default theme;